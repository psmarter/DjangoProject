# Environment Variables Configuration / 环境变量配置
# Copy this file to .env and update the values / 复制此文件为 .env 并更新配置值

# SECURITY WARNING: keep the secret key used in production secret!
# 安全警告：在生产环境中保密密钥！
# Generate a new secret key: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-here-replace-this-with-a-real-secret-key

# SECURITY WARNING: don't run with debug turned on in production!
# 安全警告：不要在生产环境中开启调试模式！
DEBUG=True

# Allowed hosts for production / 生产环境允许的主机
# Example: ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
ALLOWED_HOSTS=localhost,127.0.0.1

# Database configuration / 数据库配置
# For SQLite (default) / SQLite（默认）
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db.sqlite3

# For PostgreSQL (production recommended) / PostgreSQL（生产环境推荐）
# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=your_database_name
# DATABASE_USER=your_database_user
# DATABASE_PASSWORD=your_database_password
# DATABASE_HOST=localhost
# DATABASE_PORT=5432

# Email configuration (optional) / 邮件配置（可选）
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-email-password

# Time zone / 时区
TIME_ZONE=UTC

# Language code / 语言代码
LANGUAGE_CODE=en-us
